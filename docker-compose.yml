version: '3.7'
services:
  postgres:
    image: 'postgres:latest'
    restart: always
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=root
      - POSTGRES_DB=api
      # TODO: what password do we use for this? The one we created in the postgres shell?
      - POSTGRES_PASSWORD="76SPRNG19"
    volumes:
      - ./server/db/schema.sql:/docker-entrypoint-initdb.d/schema.sql
      - ./data:/docker-entrypoint-initdb.d/csv
  server:
    build:
      dockerfile: Dockerfile
      context: './server'
    volumes:
      - /app/node_modules
      - ./server:/app
    ports:
      - '4000:4000'
    environment:
      - PGUSER=root
      - PGHOST=postgres
      - PGDATABASE=api
      - PGPASSWORD="76SPRNG19\"
      - PGPORT=5432
volumes:
  database_data:
    driver: local
